name: Test Install

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  windows:
    name: Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # GH runners have Git and Node pre-installed.
      # Remove Node so we test the install path.
      # Git stays — we verify bash.exe discovery works with it present.
      - name: Remove Node.js from PATH
        shell: powershell
        run: |
          $nodePath = (Get-Command node -ErrorAction SilentlyContinue).Source
          if ($nodePath) {
            Write-Host "Node.js found at $nodePath — removing from PATH"
            $nodeDir = Split-Path $nodePath
            $env:PATH = ($env:PATH -split ';' | Where-Object { $_ -ne $nodeDir }) -join ';'
          }
          if (Get-Command node -ErrorAction SilentlyContinue) {
            Write-Host "Warning: node still in PATH (multiple locations)"
          } else {
            Write-Host "Node.js removed from PATH"
          }

      - name: Clean Claude Code and config
        shell: powershell
        run: |
          if (Get-Command claude -ErrorAction SilentlyContinue) {
            npm uninstall -g @anthropic-ai/claude-code 2>$null
          }
          Remove-Item Env:\CLAUDE_CODE_GIT_BASH_PATH -ErrorAction SilentlyContinue
          Remove-Item Env:\HOME -ErrorAction SilentlyContinue
          Remove-Item "$env:USERPROFILE\.claude\settings.json" -Force -ErrorAction SilentlyContinue
          Remove-Item "$env:USERPROFILE\.claude.json" -Force -ErrorAction SilentlyContinue
          Write-Host "Environment cleaned"

      - name: Run installer (no project config)
        shell: powershell
        run: |
          & .\setup.ps1

      - name: Verify Node.js
        shell: powershell
        run: |
          $v = node --version
          Write-Host "Node.js version: $v"
          $major = [int](($v -replace '^v','') -split '\.')[0]
          if ($major -lt 18) { throw "Node.js version too old: $v" }

      - name: Verify Git Bash in settings.json
        shell: powershell
        run: |
          $settingsPath = "$env:USERPROFILE\.claude\settings.json"
          if (-not (Test-Path $settingsPath)) { throw "settings.json not found" }
          $settings = Get-Content $settingsPath -Raw | ConvertFrom-Json
          $bashPath = $settings.env.CLAUDE_CODE_GIT_BASH_PATH
          if (-not $bashPath) { throw "CLAUDE_CODE_GIT_BASH_PATH not in settings.json" }
          if (-not (Test-Path $bashPath)) { throw "bash.exe not found at $bashPath" }
          if ($bashPath -like "*System32*") { throw "Got WSL bash, not Git Bash" }
          Write-Host "Git Bash verified: $bashPath"

      - name: Verify Claude Code
        shell: powershell
        run: |
          if (-not (Get-Command claude -ErrorAction SilentlyContinue)) {
            throw "claude command not found"
          }
          Write-Host "Claude Code found"

      - name: Run installer WITH project config
        shell: powershell
        run: |
          $env:CLAUDE_SETUP_CONFIG = "https://raw.githubusercontent.com/${{ github.repository }}/main/examples/igs-legacy-search.json"
          & .\setup.ps1

      - name: Verify MCP config
        shell: powershell
        run: |
          $configPath = "$env:USERPROFILE\.claude.json"
          if (-not (Test-Path $configPath)) { throw ".claude.json not found" }
          $config = Get-Content $configPath -Raw | ConvertFrom-Json
          if (-not $config.mcpServers.'igs-legacy-search') { throw "MCP server not configured" }
          $url = $config.mcpServers.'igs-legacy-search'.url
          if ($url -ne "https://malone.taildf301e.ts.net:8443/mcp") { throw "Wrong URL: $url" }
          Write-Host "MCP server verified: $url"

  macos:
    name: macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Clean Claude Code and config
        run: |
          npm uninstall -g @anthropic-ai/claude-code 2>/dev/null || true
          rm -f ~/.claude/settings.json ~/.claude.json
          echo "Environment cleaned"

      - name: Run installer (no project config)
        run: bash setup.sh

      - name: Verify Node.js
        run: |
          v=$(node --version)
          echo "Node.js version: $v"
          major=$(echo "$v" | sed 's/^v//' | cut -d. -f1)
          if [ "$major" -lt 18 ]; then exit 1; fi

      - name: Verify Claude Code
        run: |
          export PATH="$HOME/.local/bin:$HOME/.claude/bin:$HOME/.claude:$PATH"
          command -v claude || exit 1
          echo "Claude Code found"

      - name: Run installer WITH project config
        run: |
          CLAUDE_SETUP_CONFIG="https://raw.githubusercontent.com/${{ github.repository }}/main/examples/igs-legacy-search.json" \
            bash setup.sh

      - name: Verify MCP config
        run: |
          python3 -c "
          import json, sys
          c = json.load(open('$HOME/.claude.json'))
          url = c['mcpServers']['igs-legacy-search']['url']
          assert url == 'https://malone.taildf301e.ts.net:8443/mcp', f'Wrong: {url}'
          print('MCP server verified:', url)
          "

  ubuntu:
    name: Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Remove Node.js
        run: |
          sudo rm -f /usr/local/bin/node /usr/local/bin/npm /usr/local/bin/npx
          sudo rm -rf /usr/local/lib/node_modules
          hash -r
          command -v node && echo "Warning: node still found" || echo "Node.js removed"

      - name: Clean config
        run: |
          rm -f ~/.claude/settings.json ~/.claude.json
          echo "Environment cleaned"

      - name: Run installer (no project config)
        run: bash setup.sh

      - name: Verify Node.js
        run: |
          v=$(node --version)
          echo "Node.js version: $v"
          major=$(echo "$v" | sed 's/^v//' | cut -d. -f1)
          if [ "$major" -lt 18 ]; then exit 1; fi

      - name: Verify Claude Code
        run: |
          export PATH="$HOME/.local/bin:$HOME/.claude/bin:$HOME/.claude:$PATH"
          command -v claude || exit 1
          echo "Claude Code found"

      - name: Run installer WITH project config
        run: |
          CLAUDE_SETUP_CONFIG="https://raw.githubusercontent.com/${{ github.repository }}/main/examples/igs-legacy-search.json" \
            bash setup.sh

      - name: Verify MCP config
        run: |
          python3 -c "
          import json, sys
          c = json.load(open('$HOME/.claude.json'))
          url = c['mcpServers']['igs-legacy-search']['url']
          assert url == 'https://malone.taildf301e.ts.net:8443/mcp', f'Wrong: {url}'
          print('MCP server verified:', url)
          "
